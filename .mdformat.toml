wrap = 80     # wrap prose at 80 chars
number = true # consecutive 1. 2. 3. numbering

extensions = [
  "gfm",         # recognise GFM tables → not wrapped
  "frontmatter", # don't mangle YAML front matter
]

exclude = [
  "collections/**", # curated corpus — keep faithful to source
]
